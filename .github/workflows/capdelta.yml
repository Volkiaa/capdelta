# Dogfood the immutable bundled Action against this repository's own npm
# updates. pull_request_target is deliberately forbidden by PLAN §3.
name: capdelta

on:
  pull_request:
    paths:
      - package-lock.json

permissions:
  contents: read
  pull-requests: write
  security-events: write

jobs:
  review:
    name: Dependency capability review
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          persist-credentials: false
      - uses: Volkiaa/capdelta@89f87e0007b128496c5818005c884c1ac2f3ea74
        with:
          github-token: ${{ github.token }}
          fail-on: CRITICAL
