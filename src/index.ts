/**
 * M0 placeholder: gives tsc, ESLint, and vitest a compilation input so the
 * toolchain is exercised end-to-end before any application code exists.
 * Replaced by real exports at M1 (LockfileDiffer et al., PLAN §6).
 */
export const PROJECT_NAME = "capdelta";
