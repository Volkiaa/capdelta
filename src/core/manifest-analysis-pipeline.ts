/**
 * Backward-compatible M1 module path. New callers should import the
 * capability-analysis pipeline, whose name reflects both manifest and AST layers.
 */
export * from "./capability-analysis-pipeline.js";
